# -*- mode:shell-script -*-

export JAVA_HOME=/usr/lib/jvm/current
export JDK_HOME=/usr/lib/jvm/current
export PATH=$JAVA_HOME/bin:$PATH
export PATH=/sbin:$PATH:$HOME/Dropbox/ua/toolbox/bin
export LESSOPEN="| /usr/share/source-highlight/src-hilite-lesspipe.sh %s"
export LESS=' -R '
export UA_DEVO_HOME=$HOME/Dropbox/ua

if [[ $TERM != "linux" ]]; then
   export TERM=xterm-256color
fi
